---
disclaimer:
  notice: >-
    This document was generated by automated static heuristic analysis of a
    codebase snapshot. No statement, classification, or recommendation in this
    report should be taken as ground truth. Every doc lifecycle classification
    is a hypothesis derived from file-pattern matching, git metadata, and
    structural inference. Findings not backed by a concrete file reference,
    command result, or verifiable structural comparison may be incomplete.
  generated_by: "GPT-5 via Codex"
  date: "2026-05-07"
---

# Doc-Hygiene Report — framegraph

**Generated**: 2026-05-07T13:54:42-03:00
**Scope**: Full repo
**Commit / ref**: working tree (git history unavailable; unborn `main` branch)

## Executive Summary

7 documentation files were found. State counts: 1 ACTIVE, 4 DRIFTED, 2 DEPRECATED, 0 STALE, 0 ORPHANED.

**Overall posture**: the repo has a compact doc surface, but the core docs are
not currently trustworthy as a set. `README.md`, `CHANGELOG.md`, and
`CLAUDE.md` all contain high-confidence drift. The main pattern is version and
roadmap desynchronization: the code and CLI present a `2.0.0.dev0` package,
`pyproject.toml` still says `1.3.0`, the README still frames `v1.4`/`v1.5`/`v2.0`
as future roadmap items, and two planning docs are now historical artifacts.
There is very little doc automation today, so version numbers, object-type
inventories, CLI help text, and roadmap/status statements are all manual drift
surfaces.

**Top-3 urgent actions:**

1. Reconcile version truth across [README.md](/home/admin/github-mirror/_framework/framegraph/README.md), [CHANGELOG.md](/home/admin/github-mirror/_framework/framegraph/CHANGELOG.md), [framegraph/__init__.py](/home/admin/github-mirror/_framework/framegraph/framegraph/__init__.py), and [pyproject.toml](/home/admin/github-mirror/_framework/framegraph/pyproject.toml).
2. Repair [README.md](/home/admin/github-mirror/_framework/framegraph/README.md): add the required disclaimer link, remove stale roadmap language, and regenerate the object-type surface from the live renderer registry.
3. Archive superseded planning docs [docs/bar.md](/home/admin/github-mirror/_framework/framegraph/docs/bar.md) and [docs/solution-plan.md](/home/admin/github-mirror/_framework/framegraph/docs/solution-plan.md) so they stop competing with the shipped state.

## Documentation Inventory

| # | Path | Type | State | Confidence | Last Modified | Lines | Signal |
|---|---|---|---|---|---|---|---|
| 1 | `CLAUDE.md` | markdown | DRIFTED | HIGH | unknown (untracked tree) | 344 | Generic template block still present; mandates `@PURPOSE.md`, but no `PURPOSE.md` exists |
| 2 | `README.md` | markdown | DRIFTED | HIGH | unknown (untracked tree) | 181 | Missing required disclaimer link; roadmap and object inventory lag live code |
| 3 | `CHANGELOG.md` | markdown | DRIFTED | HIGH | unknown (untracked tree) | 229 | Release/version narrative conflicts with manifest and CLI version output |
| 4 | `DISCLAIMER.md` | markdown | ACTIVE | HIGH | unknown (untracked tree) | 210 | Canonical disclaimer file exists and is internally coherent |
| 5 | `docs/bar.md` | markdown | DEPRECATED | HIGH | unknown (untracked tree) | 155 | Historical Phase 0 gate; claims no CI/pyproject/tests, all now false |
| 6 | `docs/proposal-python-code-style.md` | markdown | DRIFTED | HIGH | unknown (untracked tree) | 278 | Proposal baseline already partially outdated (`.pre-commit-config.yaml` now exists) |
| 7 | `docs/solution-plan.md` | markdown | DEPRECATED | HIGH | unknown (untracked tree) | 338 | Future-state roadmap superseded by shipped `v1.5`/`2.0.0-dev` features |

### State legend

- **ACTIVE**: Currently relevant, content matches code.
- **DRIFTED**: Was active, now contains inaccuracies.
- **DEPRECATED**: Superseded by later work or current repo state.

### Confidence legend

- **HIGH**: Confirmed by direct file/content mismatch or missing referenced artifact.

## Active Doc Sync Results

### `README.md`

| Check | Verdict | Detail |
|---|---|---|
| Description match | PASS | README tagline matches [pyproject.toml](/home/admin/github-mirror/_framework/framegraph/pyproject.toml) project description at a high level |
| Installation commands | PASS | `pip install framegraph` and `pip install "framegraph[test]"` match package name and optional extras |
| CLI usage | PASS | `render`, `deck`, and `version` match `python3 -m framegraph.cli --help` |
| Usage examples | PASS | Public imports `FrameGraphRenderer`, `FrameGraphLibrary`, `FrameGraphDeckRenderer` exist in [framegraph/__init__.py](/home/admin/github-mirror/_framework/framegraph/framegraph/__init__.py) |
| Required disclaimer link | DRIFT | `CLAUDE.md` requires every README to reference `@DISCLAIMER.md`; root README does not |
| Object-type inventory | DRIFT | Table omits shipped types such as `bullet_list`, `container`, `bar_chart`, and `line_chart` present under `framegraph/renderers/` and test fixtures |
| Version / roadmap narrative | DRIFT | README still frames `v1.4`, `v1.5`, and `v2.0` as future, while `CHANGELOG.md` and the codebase already expose those features/dev status |
| License / provenance surface | SKIP | README says `MIT`, but the repo has no `LICENSE*` file to validate against |

**Sync score**: 4/8 checks passing.

### `CHANGELOG.md`

| Check | Verdict | Detail |
|---|---|---|
| Latest version alignment | DRIFT | [pyproject.toml](/home/admin/github-mirror/_framework/framegraph/pyproject.toml) says `1.3.0`, `python3 -m framegraph.cli version` prints `2.0.0.dev0`, and the first changelog release heading is `2.0.0-dev` |
| Unreleased section truth | DRIFT | `[Unreleased]` still lists items already marked `[SHIPPED]`, so it mixes backlog and completed work in one section |
| Feature claims vs code | PASS | Features called out for `bar_chart`, `line_chart`, `notes`, `container`, and `register(...)` are reflected in live modules and tests |
| Changelog automation | PASS | No generator is configured; current manual maintenance posture is accurately implicit rather than falsely claimed automated |

**Sync score**: 2/4 checks passing.

### `CLAUDE.md`

| Check | Verdict | Detail |
|---|---|---|
| Project overview block | DRIFT | The `[PROJECT] Replace this block...` template text is still present |
| Required project references | DRIFT | The file mandates `@PURPOSE.md`, but the repo has no `PURPOSE.md` |
| README disclaimer rule | PASS | The rule itself is clear and actionable; the drift is in repo compliance, not the rule text |

**Sync score**: 1/3 checks passing.

### `DISCLAIMER.md`

| Check | Verdict | Detail |
|---|---|---|
| Canonical disclaimer presence | PASS | Root disclaimer exists and is suitable for the README reference model required by `CLAUDE.md` |

**Sync score**: 1/1 checks passing.

### `docs/proposal-python-code-style.md`

| Check | Verdict | Detail |
|---|---|---|
| CI / mypy posture | PASS | Proposal correctly describes mypy as advisory in [.github/workflows/ci.yml](/home/admin/github-mirror/_framework/framegraph/.github/workflows/ci.yml) |
| `pyproject.toml` baseline | PASS | Proposal correctly cites `strict = false` and the Ruff line-length target |
| Pre-commit status | DRIFT | Proposal says `.pre-commit-config.yaml` is absent, but the file now exists at repo root |
| Public API docstring gap | DRIFT | Proposal claims renderer entry points lack docstrings, but `framegraph/renderers/*.py` now contains module/function documentation for the live renderers |

**Sync score**: 2/4 checks passing.

## Quarantine Proposals

Docs proposed for retirement. Review before executing.

### DEPRECATED

| # | Path | Evidence | Recommended Action | TL;DR |
|---|---|---|---|---|
| 1 | `docs/bar.md` | Declares itself `phase: 0`; says `pyproject.toml` skeleton, CI, and tests are pending; references `framegraph_to_svg_v3.py` | Archive | Phase-0 bar-definition memo for an earlier planning cycle; useful as provenance, misleading as live guidance |
| 2 | `docs/solution-plan.md` | Frames `v1.4`, `v1.5`, and `v2.0` as future roadmap phases already reflected elsewhere as shipped or in-dev | Archive | Historical roadmap from the `v1.3` era; keep as planning history, not active documentation |

## Automation Recommendations

| # | Doc | Element | Current State | Recommended Tool | Hook Point | Drift Risk if Manual |
|---|---|---|---|---|---|---|
| 1 | `README.md` | CLI usage block | Hand-written | `python3 -m framegraph.cli --help` + marker-based injection script | Pre-commit and CI | Flags/subcommands drift as soon as CLI changes |
| 2 | `README.md` | Object-type inventory | Hand-written table | Small Python script that imports renderer registries from `framegraph/renderers/*.py` and renders Markdown | Pre-commit | New object types ship undocumented or old ones remain listed |
| 3 | `README.md`, `CHANGELOG.md`, package metadata | Version string | Split across `pyproject.toml`, `framegraph/__init__.py`, README, changelog | `setuptools-scm` or a single-source version script | Release workflow and CI | Package, CLI, README, and changelog disagree about the release line |
| 4 | `CLAUDE.md` / repo health | Template placeholder and required-file checks | Unchecked manual contract | Simple `rg`-based docs lint script (`[PROJECT]`, `@PURPOSE.md`, README disclaimer link) | CI | Generic template text and missing required docs stay undetected |
| 5 | Planning docs under `docs/` | Lifecycle status | Free-form | Archive convention plus CI check for `status: draft` / `phase: 0` docs outside `docs/archive/` | CI | Historical plans continue to masquerade as current guidance |

### Detailed recommendations

#### Recommendation #1: auto-generate README CLI usage

**What**: Replace the hand-maintained CLI synopsis in `README.md` with output
captured from the real parser.

**How**:
1. Add `<!-- cli:start -->` / `<!-- cli:end -->` markers to `README.md`.
2. Add a small script under `scripts/` that runs `python3 -m framegraph.cli --help`,
   extracts the command synopsis, and rewrites the marked block.
3. Fail CI when rerunning the script produces a diff.

**What it replaces**: The current manual CLI block in `README.md`.

**Drift risk if not automated**: Every new flag, alias, or usage-text change
requires a second manual edit and will silently drift.

#### Recommendation #2: generate the README object inventory from renderer registries

**What**: Build the README object-type table from `RENDERERS` registries in
`framegraph/renderers/`.

**How**:
1. Import `framegraph.renderers.*` in a small script and collect the registry keys.
2. Map each key to a short description in one maintained data structure.
3. Inject the rendered Markdown table into a marked section in `README.md`.

**What it replaces**: The current hand-maintained object-type matrix.

**Drift risk if not automated**: The doc surface will keep lagging behind
shipped object types such as `bullet_list`, `container`, and charts.

#### Recommendation #3: enforce a single release-version source of truth

**What**: Stop maintaining version identifiers independently in code,
manifest, README, and changelog.

**How**:
1. Choose one source of truth: `setuptools-scm` or a repo-local version bump script.
2. Generate or validate `framegraph/__init__.py::__version__` from that source.
3. Add a CI check that compares the package version, CLI `version` output, and
   latest changelog heading.

**What it replaces**: Manual multi-file version edits.

**Drift risk if not automated**: Users cannot tell whether the package is
`1.3.0`, `1.5.0`, or `2.0.0.dev0`.

## Quarantine Artifacts

The following files were generated alongside this report:

- [doc-quarantine.sh](/home/admin/github-mirror/_framework/framegraph/doc-quarantine.sh) — commented shell script to move proposed docs into `.doc-quarantine/`
- [quarantine-manifest.json](/home/admin/github-mirror/_framework/framegraph/quarantine-manifest.json) — machine-readable quarantine data
- [quarantine-summaries.md](/home/admin/github-mirror/_framework/framegraph/quarantine-summaries.md) — TL;DR summaries for institutional memory

## Methodology Notes

### What was inspected

- Root docs: `README.md`, `CHANGELOG.md`, `CLAUDE.md`, `DISCLAIMER.md`
- `docs/` markdown files
- Python package metadata and exports in `pyproject.toml`, `framegraph/__init__.py`, and `framegraph/cli.py`
- CI configuration in `.github/workflows/ci.yml`
- Repo file layout, tests, fixtures, and renderer modules

### What was NOT inspected

- External docs outside the repository
- Binary docs (none discovered)
- Semantic accuracy of every prose claim inside large planning docs
- Inline docstrings beyond a bulk posture check

### Heuristic limitations

This repo currently has no usable git history in the checkout: `git log` fails
because `main` is unborn and every file is untracked. That removes normal
age/co-maintenance signals, so this audit relies primarily on direct structural
truth checks against the current tree and live command output.
