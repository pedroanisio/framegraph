# Python build / install artifacts
*.pyc
__pycache__/
*.egg-info/
.coverage
.coverage.*
.hypothesis/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Local virtual env
.venv/

# Editor / agent-local state
.claude/

# Build outputs (rendered SVGs from running the CLI)
output/

# Documentation portal — generated source tree + built static site.
# Regenerated from docstrings / schema / examples by `make portal`.
docs/portal/
site/

# Agent-skill metadata (not part of project source)
.repo/
.agent-tasks/
.env
